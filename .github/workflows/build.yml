name: VSIX + test

permissions:
  contents: read
on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:
    strategy:
      matrix:
        os: [macos-latest, windows-latest, ubuntu-latest]
    runs-on: ${{ matrix.os }}
    
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24.0.0

      - name: Dependencies
        run: 
          npm install

      - name: Test (Ubuntu)
        if: contains(matrix.os, 'ubuntu')
        run: xvfb-run npm test

      - name: Test (macOS)
        if: matrix.os == 'macos-latest'
        run: |
          rm -rf .vscode-test .vscode-u
          echo "DEBUG: listing .vscode-test (if present)"
          ls -la .vscode-test || true
          echo "DEBUG: finding files under .vscode-test"
          find .vscode-test -maxdepth 5 -ls || true
          npm test

      - name: Test (Windows)
        if: matrix.os == 'windows-latest'
        shell: powershell
        run: |
          Remove-Item -Path .vscode-test,.vscode-u -Recurse -Force -ErrorAction SilentlyContinue
          npm test
